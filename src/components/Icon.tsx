import React from 'react';
import {Icon} from '@ui-kitten/components';

const Home = (style: any) => <Icon {...style} name="home" pack="assets" />;

const Virus = (style: any) => <Icon {...style} name="virus" pack="assets" />;

const Stats = (style: any) => <Icon {...style} name="info" />;

export default {Home, Virus, Stats};
